import AppSidebar from "../../../../../../components/AppSidebar";

const Settings = () => {
  return (
    <div className="flex min-h-screen">
      <AppSidebar />
    </div>
  );
};

export default Settings;
